import { VowelToNumberPipe } from './vowel-to-number.pipe';

describe('VowelToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new VowelToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
